# 安装部署

## Docker 部署（推荐）

### docker-compose

```yaml
version: "3"
services:
  faststrm:
    image: wabisabi926/faststrm:latest
    container_name: faststrm
    ports:
      - "3000:3000"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/logs
      - /path/to/your/strm:/app/data/strm  # STRM 文件输出目录
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
```

```bash
docker-compose up -d
```

### docker run

```bash
docker run -d \
  --name faststrm \
  -p 3000:3000 \
  -v ./config:/app/config \
  -v ./data:/app/data \
  -v ./logs:/app/logs \
  -v /path/to/your/strm:/app/data/strm \
  -e TZ=Asia/Shanghai \
  --restart unless-stopped \
  wabisabi926/faststrm:latest
```

---

## 端口说明

| 端口 | 用途 |
|------|------|
| 3000 | Web UI + STRM 路由 API（`/api/strm`） |

> 💡 仅需暴露 3000 端口。宿主机反向代理不需要额外端口。

---

## 目录挂载

| 目录 | 容器路径 | 用途 |
|------|---------|------|
| `./config` | `/app/config` | 配置文件（config.json, settings.json, account.json, tasks.json） |
| `./data` | `/app/data` | 数据文件（filePathDb.sqlite, 删除历史, 生活事件日志） |
| `./logs` | `/app/logs` | 应用日志 |
| STRM 目录 | `/app/data/strm` | STRM 文件输出路径 |

---

## 配置文件说明

| 文件 | 用途 | 敏感字段 |
|------|------|---------|
| `config.json` | 登录凭据 | 密码 salt+SHA-256 |
| `settings.json` | 全局设置 | apiKey/botToken AES-256-GCM |
| `account.json` | 115 账号信息 | cookie/token AES-256-GCM |
| `tasks.json` | 同步任务定义 | - |

> 首次启动会从内置默认模板自动生成配置文件。

---

## 首次登录

1. 浏览器打开 `http://localhost:3000`
2. 默认账号 `admin` / 密码 `admin`
3. 登录后请立即在「设置」页面修改密码

---

## 版本升级

```bash
docker-compose pull
docker-compose up -d
```

配置文件向后兼容，无需手动迁移。

### Docker 镜像标签

| 标签 | 说明 |
|------|------|
| `latest` | 最新版本 |
| `v0.8.3` | 指定版本（推荐生产用） |

---

## 反向代理（可选）

如需公网访问，建议在前面加 Nginx/Caddy：

```nginx
server {
    listen 443 ssl;
    server_name strm.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

> 💡 STRM 路由需要正确传递客户端 IP（用于局域网代理判断），务必配置 `X-Forwarded-For` 头。

---

## 构建架构

GitHub Actions 自动构建以下平台：

- `linux/amd64`（x86_64）
- `linux/arm64`（Apple Silicon / 树莓派 4 / CoreELEC）
